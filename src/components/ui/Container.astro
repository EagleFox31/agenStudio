---
interface Props {
  as?: string;
  class?: string;
}
const { as: Tag = 'div', class: className = '' } = Astro.props;
---

<Tag class:list={['max-w-content mx-auto px-4 sm:px-6 lg:px-8 w-full', className]}>
  <slot />
</Tag>
